// The resources of the client, packaged in the released bundle.

const pages = {
	pageReviewingAndPlaning: 'pages/reviewing-and-planing.html',
};

const icons = {
	app: 'icon.png',
};

export const AppPages = pages;
export const AppIcons = icons;

